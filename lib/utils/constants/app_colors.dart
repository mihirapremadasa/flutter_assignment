import 'package:flutter/material.dart';

class AppColors {
  //------to store colors used in the app

  static const primaryColor = Color(0xff0FA956);
  static const ash = Color(0xff989898);
  static const white = Colors.white;
}
